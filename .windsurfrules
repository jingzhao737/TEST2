# Windsurf Rules — Portfolio v3

Read the following files before doing ANY work:
1. `AGENTS.md` — Workflow rules, backup commands, token 节省规则, and behavioral guidelines
2. `PROJECT.md` — Full project architecture, design decisions, and user preferences

# ✦ Token 节省规则（核心） ✦

1. **精确读取文件。** `view_file` 必须指定 StartLine/EndLine，每次限 20-50 行。禁止一次读 200+ 行。先用 `findstr` 或 `grep_search` 定位行号再读取。
2. **简单任务直接做。** 改颜色、改数值、修 bug 等 ≤3 文件的改动不需要写 plan/task/walkthrough，直接编辑+回复总结即可。
3. **Git 搜索要节制。** 用 `--oneline` 而非 `-p`。需要看具体 diff 时用 `git show <hash> -- <file>` 限定单文件。
4. **不维护冗余文档。** `task.md`、`walkthrough.md`、`implementation_plan.md` 只在复杂任务中创建。简单任务完成后回复中总结即可。
5. **重型搜索交给 subagent。** 需要大量 grep/阅读多文件的调研任务委派给 `research` subagent。
6. **命令输出要精简。** 避免不加 `-n` 的 `git log`、不加 filter 的大日志命令。

# ✦ 语言规则 ✦
1. 与用户沟通统一使用中文。
2. 代码注释可以用英文。
